<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jns.chefboard.dao.ChefBoardDAO">

	<resultMap type="com.jns.chefboard.vo.ChefBoardVO" id="cbvo">
		<result property="bno" column="BNO"/>
		<result property="btype" column="BTYPE"/>
		<result property="btitle" column="BTITLE"/>
		<result property="bcontent" column="BCONTENT"/>
		<result property="bnick" column="BNICK"/>
		<result property="bfile" column="BFILE"/>
		<result property="binsertdate" column="BINSERTDATE"/>
		<result property="bupdatedate" column="BUPDATEDATE"/>
		<result property="bview" column="BVIEW"/>
		<result property="bdeleteyn" column="BDELETEYN"/>
		<result property="bhits" column="BHITS"/>
	</resultMap>

	<insert id="boardInsert" parameterType="cbvo">
		INSERT INTO BOARD(
						  BNO
						 ,BTYPE
						 ,BTITLE
						 ,BCONTENT
						 ,BNICK
						 ,BFILE
						 ,BINSERTDATE
						 ,BUPDATEDATE
						 ,BVIEW
						 ,BDELETEYN
						 ,BHITS
						 )
		VALUES			 (
						  #{bno, jdbcType=VARCHAR}
						 ,'C'
						 ,#{btitle, jdbcType=VARCHAR}
						 ,#{bcontent, jdbcType=VARCHAR}
						 ,#{bnick, jdbcType=VARCHAR}
						 ,#{bfile, jdbcType=VARCHAR}
						 ,SYSDATE
						 ,SYSDATE
						 ,0
						 ,'Y'
						 ,0
						 )
	</insert>
	
	<select id="boardList" parameterType="cbvo" resultType="cbvo">
		SELECT *
		FROM BOARD A
		WHERE A.BDELETEYN = 'Y'
		<if test="startdate!=null and startdate!='' and enddate!=null and enddate!=''">
		<![CDATA[AND TO_CHAR(A.BINSERTDATE, 'YYYY-MM-DD') >= TO_CHAR(TO_DATE(#{startdate, jdbcType=VARCHAR}), 'YYYY-MM-DD')]]>
		<![CDATA[AND TO_CHAR(A.BINSERTDATE, 'YYYY-MM-DD') <= TO_CHAR(TO_DATE(#{enddate, jdbcType=VARCHAR}), 'YYYY-MM-DD')]]>
		</if>
		<if test="keyfilter=='key1'">
			<![CDATA[AND A.BTITLE LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%']]>
		</if>
		<if test="keyfilter=='key2'">
			<![CDATA[AND A.BCONTENT LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%']]>
		</if>
		<if test="keyfilter=='key3'">
			<![CDATA[AND A.BTITLE LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%']]>
			<![CDATA[AND A.BCONTENT LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%']]>
		</if>
		<if test="keyfilter=='key4'">
			<![CDATA[AND A.BNICK LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%']]>
		</if>
		<if test="keyfilter=='key5'">
			<![CDATA[AND A.BNO LIKE '%' || #{keyword, jdbcType=VARCHAR} || '%']]>
		</if>
		ORDER BY 1
	</select>
	
</mapper>